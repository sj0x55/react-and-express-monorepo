export enum ProductType {
  DISKS = 'disks',
  SMARTPHONES = 'smartphones',
  OTHER = 'other',
};

export enum ProductCondition {
  NEW = 'new',
  USED = 'used',
};

export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  OPTIONS = 'OPTIONS'
};

export enum NodeEnv {
  DEV = 'development',
  PROD = 'production',
};